export enum OrderState {
    CREATED = "CREATED",
    ANALYSIS = "ANALYSIS",
    COMPLETED = "COMPLETED"
}

export enum Status {
    ACTIVE = "ACTIVE",
    DELETED = "DELETED",
    PENDING = "PENDING",
    DONE = "DONE"
}